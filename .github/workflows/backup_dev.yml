name: Backup DEV Database

on:
  schedule:
    - cron: '*/15 14-23,0-2 * * *'
  workflow_dispatch:

jobs:
  backup-dev:
    runs-on: ubuntu-latest
    steps:
      - name: Install Postgres 17 Tools
        run: |
          # 1. Add the official PostgreSQL repository
          sudo sh -c 'echo "deb http://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" > /etc/apt/sources.list.d/pgdg.list'
          wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo apt-key add -
          
          # 2. Install Version 17
          sudo apt-get update
          sudo apt-get install -y postgresql-client-17
          
          # 3. CRITICAL: Force the runner to use v17 instead of v16
          echo "/usr/lib/postgresql/17/bin" >> $GITHUB_PATH

      - name: Verify Version
        run: pg_dump --version  # This should now say 17.x

      - name: Install & Configure Rclone
        run: |
          sudo -v ; curl https://rclone.org/install.sh | sudo bash
          mkdir -p ~/.config/rclone
          echo "${{ secrets.RCLONE_CONFIG }}" > ~/.config/rclone/rclone.conf

      - name: Backup DEV to Google Drive
        env:
          DB_URL: ${{ secrets.SUPABASE_DEV_DB_URL }}
        run: |
          mkdir -p backups
          TIMESTAMP=$(date +%Y-%m-%d_%H-%M)
          
          echo "Backing up Dev..."
          pg_dump "$DB_URL" --clean --if-exists --no-owner --file=backups/dev_$TIMESTAMP.sql
          
          echo "Uploading to Google Drive..."
          rclone copy backups/ gdrive:PointTracker_Backups/Dev
          
          echo "✅ Dev Backup Success!"
